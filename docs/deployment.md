# Deployment guide

Run the complete quality suite, then deploy with the Sites hosting workflow. The project uses the bundled vinext structure and Cloudflare-compatible output. No database or runtime secrets are required in version one. Update the canonical site URL and social metadata after a production domain is assigned.
