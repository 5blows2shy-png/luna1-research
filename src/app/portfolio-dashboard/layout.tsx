import type { Metadata } from "next";
export const metadata:Metadata={title:"Portfolio Dashboard",description:"An educational investment-committee dashboard for positions, research priorities, allocation, performance, and decision reviews."};
export default function PortfolioDashboardLayout({children}:{children:React.ReactNode}){return children}
