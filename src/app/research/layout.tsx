import type { Metadata } from "next";
export const metadata:Metadata={title:"Equity Research",description:"Luna1 company research organized around business quality, change, valuation, institutional behavior, technical structure, and risk."};
export default function ResearchLayout({children}:{children:React.ReactNode}){return children}
