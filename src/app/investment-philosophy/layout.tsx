import type { Metadata } from "next";
export const metadata:Metadata={title:"Investment Philosophy",description:"Principles for evidence-based research, capital allocation, thesis invalidation, and continuous process improvement."};
export default function PhilosophyLayout({children}:{children:React.ReactNode}){return children}
