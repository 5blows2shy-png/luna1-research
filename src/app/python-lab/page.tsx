import type { Metadata } from "next";
import { LuxuryCard, MetricCard } from "@/components/luxury";
import { PageHeader, SectionHeading } from "@/components/site";
import { pythonProjects } from "@/lib/lab-data";

export const metadata:Metadata={title:"Python Lab",description:"Auditable Python projects supporting investment research, financial-model controls, and professional document workflows."};
export default function PythonLab(){return <><PageHeader kicker="Analytical Systems" title="Small tools that make financial judgment more repeatable." description="A portfolio of transparent Python workflows for research prioritization, financial controls, and document quality—not automated trading."/><section><div className="placeholder-banner">Educational project portfolio. No brokerage execution, live recommendations, or private API credentials are exposed.</div><div className="metric-strip"><MetricCard label="Projects" value={String(pythonProjects.length)}/><MetricCard label="Completed" value={String(pythonProjects.filter(project=>project.status==="Completed").length)}/><MetricCard label="Primary principle" value="Auditable"/><MetricCard label="Trading access" value="None"/></div></section><section><SectionHeading eyebrow="Project Ledger" title="Tools designed for review, not black-box certainty."/><div className="python-project-grid">{pythonProjects.map((project,index)=><LuxuryCard variant="portfolio" key={project.slug}><div className="project-index"><span>{String(index+1).padStart(2,"0")}</span><small>{project.status}</small></div><h2>{project.name}</h2><p>{project.description}</p><dl><div><dt>Stack</dt><dd>{project.stack.join(" · ")}</dd></div><div><dt>Output</dt><dd>{project.output}</dd></div><div><dt>Control</dt><dd>{project.control}</dd></div></dl></LuxuryCard>)}</div></section><section className="python-console"><div><span className="eyebrow">Control Philosophy</span><h2>Readable inputs. Reconciled outputs. Human accountability.</h2><p>The lab favors small, typed workflows with explicit assumptions and tests. Automation supports the analyst; it does not replace source review or judgment.</p></div><pre aria-label="Illustrative model control output"><code>{`LUNA1 / MODEL CONTROL\nstatus       PASS\nsources      RECONCILED\nassumptions  LABELED\nrecommendation NONE\nreview       REQUIRED`}</code></pre></section></>}
