import type { Metadata } from "next";
export const metadata:Metadata={title:"About",description:"The professional background, research focus, and operating perspective behind Luna1 Research."};
export default function AboutLayout({children}:{children:React.ReactNode}){return children}
