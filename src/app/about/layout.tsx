import type { Metadata } from "next";
export const metadata:Metadata={title:"About",description:"The professional background, research focus, and operating perspective behind Klyro."};
export default function AboutLayout({children}:{children:React.ReactNode}){return children}
