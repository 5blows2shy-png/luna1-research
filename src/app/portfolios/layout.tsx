import type { Metadata } from "next";
export const metadata:Metadata={title:"Portfolio",description:"Luna1 portfolio theses, research priorities, long-term holdings, allocation framework, illustrative performance, and decision reviews."};
export default function PortfolioLayout({children}:{children:React.ReactNode}){return children}
