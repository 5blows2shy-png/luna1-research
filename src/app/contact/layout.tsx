import type { Metadata } from "next";
export const metadata:Metadata={title:"Contact",description:"Request a professional connection with Shy Lee for recruiting, investment research, finance, or collaboration."};
export default function ContactLayout({children}:{children:React.ReactNode}){return children}
