import type {MetadataRoute} from "next";
import {commentary,research} from "@/lib/data";
const routes=["","/about","/brand","/certifications","/contact","/financial-models","/investment-philosophy","/luna1-framework","/market-commentary","/portfolio-dashboard","/portfolios","/research","/resume","/valuation-models"];
export default function sitemap():MetadataRoute.Sitemap{const base="https://luna1-research.vercel.app";return [...routes.map(route=>({url:`${base}${route}`,changeFrequency:route===""?"weekly" as const:"monthly" as const,priority:route===""?1:.7})),...research.map(item=>({url:`${base}/research/${item.ticker.toLowerCase()}`,changeFrequency:"monthly" as const,priority:.6})),...commentary.map(item=>({url:`${base}/market-commentary/${item.slug}`,changeFrequency:"monthly" as const,priority:.5}))]}
