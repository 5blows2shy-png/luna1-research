import {notFound} from "next/navigation";import {commentary} from "@/lib/data";import {PageHeader} from "@/components/site";
export function generateStaticParams(){return commentary.map(x=>({slug:x.slug}))};
export default async function Post({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=commentary.find(c=>c.slug===slug);if(!x)notFound();return <><PageHeader kicker={`${x.category} · ${x.date}`} title={x.title} description={x.summary}/><article className="prose"><div className="placeholder-banner">Illustrative commentary — not current market data or investment advice.</div><h2>Key observations</h2><p>Leadership is best evaluated beneath headline index levels. Breadth, relative strength, earnings response, and volume reveal where institutions are concentrating capital.</p><h2>What changed</h2><p>The balance of evidence has shifted toward businesses with visible demand, resilient margins, and improving estimates. This observation is a research prompt, not a market forecast.</p><h2>What to watch</h2><p>Watch follow-through after earnings, participation across industries, and whether emerging leaders hold constructive technical structure during market pullbacks.</p><h2>Relevant sectors and tickers</h2><p>AI infrastructure, cybersecurity, specialty construction, and select consumer compounders. Sample study list: ANET, STRL, PANW, CASY.</p></article></>}
