import {test,expect} from "@playwright/test";
test("primary pages load",async({page})=>{for(const path of ["/","/research","/portfolio-dashboard","/financial-models","/contact"]){await page.goto(path);await expect(page.locator("h1")).toBeVisible()}});
test("contact form and endpoint validate",async({page,request})=>{await page.goto("/contact");await expect(page.getByRole("button",{name:"Request connection"})).toBeVisible();const response=await request.post("/api/contact",{data:{name:"Research Visitor",organization:"Example Organization",reason:"Research discussion",message:"I would like to discuss the educational research methodology."}});expect(response.ok()).toBeTruthy()});
test("commercial plays with accessible controls",async({page})=>{await page.goto("/commercial");await expect(page.getByRole("heading",{name:/From market noise/})).toBeVisible();await expect(page.getByRole("button",{name:"Play Luna1 commercial"})).toBeVisible();await page.getByRole("button",{name:"Play Luna1 commercial"}).click();await expect(page.getByRole("button",{name:"Pause commercial"})).toBeVisible();await page.getByRole("slider",{name:"Commercial timeline"}).fill("16");await expect(page.getByText("Evidence, operationalized.")).toBeVisible();await page.getByRole("button",{name:"Pause commercial"}).click();await expect(page.getByRole("button",{name:"Play commercial"})).toBeVisible();await page.getByRole("button",{name:"Restart commercial"}).click()});
